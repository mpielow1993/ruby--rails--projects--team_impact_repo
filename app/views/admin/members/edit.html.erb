<% provide(:title, "Edit Member") %> 
<% provide(:button_text, "Save Changes") %>
    <h1>Update your profile</h1> 
        <div class="row"> 
            <div class="col-md-6 col-md-offset-3"> 
                <%= render 'layouts/admin_member_edit_form' %>
            </div>
        </div>
