<%= form_for @programme, url: admin_programme_path(@programme), method: :patch do |f| %>

<%-#Make this a multi-part form to handle file attachments%>
    <%= render 'shared/error_messages', object: f.object, html: { multipart: true } %>

    <h3><%= f.label :name, "Name" %></h3>
    <%= f.text_field :name, class: "form-control" %>

    <h3><%= f.label :quotation, "Quotation (Optional)" %></h3>
    <%= f.text_field :quotation, class: "form-control" %>

    <h3><%= f.label :programme_avatar, "Programme Avatar" %> </h3>
    <%= f.file_field :programme_avatar, class: "form-control" %>

    <% if @programme.programme_avatar? %>
        <h3><%= f.label :remove_programme_avatar, "Remove Programme Avatar" %> </h3>
        <%= f.check_box :remove_programme_avatar %>
    <% end %>

    <%= f.submit yield(:button_text), class: "btn btn-primary" %>

<% end %>
